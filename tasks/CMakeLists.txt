# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sysmonTaskLib)
ADD_LIBRARY(${SYSMON_TASKLIB_NAME} SHARED
    ${SYSMON_TASKLIB_SOURCES})
add_dependencies(${SYSMON_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${SYSMON_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SYSMON_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SYSMON_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SYSMON_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SYSMON_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SYSMON_TASKLIB_INTERFACE_LIBRARIES}")

orogen_apply_task_library_options(${SYSMON_TASKLIB_NAME})

INSTALL(TARGETS ${SYSMON_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SYSMON_TASKLIB_HEADERS}
    DESTINATION include/orocos/sysmon)

